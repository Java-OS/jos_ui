import 'package:flutter/material.dart';

class EnvironmentComponent extends StatelessWidget {
  const EnvironmentComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Content');
  }
}
